version: '3'

services:
  cyber:
    container_name: cyber
    image: innei/cyber:latest
    volumes:
      - ./.env:/app/apps/web/.env
      - ./public:/app/apps/web/public
    restart: always
    ports:
      - 2323:2323
